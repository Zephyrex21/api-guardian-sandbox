# Points the dashboard at your backend. For local dev this is the default
# already (http://localhost:3000) so you can leave this blank locally.
# Once deployed, set this to your real Render URL, e.g.
# https://api-guardian.onrender.com
VITE_API_URL=
